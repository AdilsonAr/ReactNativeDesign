import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
    imageDimention: {
        height: 230,
        width: 200,
        alignSelf: "flex-end",
    }
})

export default estilos